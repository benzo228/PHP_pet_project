#Тут лежит секрет

grafana_admin_password: "admin"

#создать - ansible-vault create group_vars/all/vault.yml
#редактирование - ansible-vault edit group_vars/all/vault.yml
#запуск - ansible-playbook -i localhost, deploy-monitoring.yml --ask-vault-pass
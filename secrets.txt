Секреты PHP pet проекта

Redis
db_password: petpass

PostgreSQL:
postgres_password: petpass
(Сам образ Postgres не умеет брать пароль из файла, только из переменной. Поэтому передаю из файла в переменную в stack)